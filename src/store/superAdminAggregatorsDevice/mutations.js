export const SET_CREATED_AGGREGATORS_DEVICE_LIST = (state, payload) => {
    state.getAggregatorDeviceList = payload;
};

// SET_GET_ACTIVE_CREATED_DEVICE_LIST

export const SET_GET_ACTIVE_CREATED_DEVICE_LIST = (state, payload) => {
    state.getActiveAggregatorDeviceList = payload;
};