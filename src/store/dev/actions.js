// Empty Vuex actions for now, ready for async/await usage
export const someAction = async (_, payload) => {
  // Placeholder for future API calls or actions
  // Example:
  // const response = await api.get('/your-endpoint')
  // commit('SOME_MUTATION', response.data)
  return payload
}