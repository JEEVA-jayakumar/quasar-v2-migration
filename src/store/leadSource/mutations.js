export const SET_LEAD_SOURCE_ACTIVE_LIST = (state, payload) => {
  state.activeLeadSource = payload;
};
export const SET_LEAD_SOURCE_DEACTIVED_LIST = (state, payload) => {
  state.deActivatedLeadSource = payload;
};
